import React from "react"
import ReactDOM from "react-dom"
import TimerWrapper from "./App"

ReactDOM.render(
  <React.StrictMode>
    <TimerWrapper />
  </React.StrictMode>,
  document.getElementById("root")
)